# Variables

**Descriptions**:

Variables are like containers for storing data. It can store all the data types that python provide. Variables can be defined in python by giving them a name and then passing a value into it. The value has to be in the format that suits the data type that you are using.

**Example**:

```python
example = "String" # string data type

print(example) # prints the `String`
```
